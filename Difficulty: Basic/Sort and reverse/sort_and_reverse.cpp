// User function Template for C++

void sortAndReverse(vector<int> &arr) {
    sort(arr.begin(), arr.end());     // Sort in ascending order
    reverse(arr.begin(), arr.end());  // Reverse to get descending order
}
