// User function Template for C++

vector<pair<int, pair<int, int>>> increasing_List(vector<pair<int, pair<int, int>>> v) {
    // Sort using default comparator (lexicographical order)
    sort(v.begin(), v.end());
    return v;
}
